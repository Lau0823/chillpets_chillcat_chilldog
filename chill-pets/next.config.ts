/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "placedog.net",
      "placekitten.com",
      "i.pinimg.com",
      "images.unsplash.com",
    ],
  },
}

module.exports = nextConfig
